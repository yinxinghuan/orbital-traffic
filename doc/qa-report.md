# Orbital Traffic 视觉 QA

日期：2026-07-23

## 结果

- 390×844：3,500 条航班、2,048px 地球纹理、标题/读数/44px 隐藏动作提示均未遮挡视觉主体。
- 320×568：地球保持主要构图，标题与 3,500 航班读数不碰撞，底部提示完整。
- 1440×900：7,000 条航班与产品 2,048px 纹理加载正确，地球居中并保留自由空间。
- 单指拖动：相机由 `[-3536.6, 2077.5, 4379.1]` 变为 `[1145.7, -1601.1, 5667.8]`，OrbitControls 自由旋转保留。
- 真实双指静止：进入 `frozen`，速度倍率 `0.003`、路径透明度 `0.82`、地球亮度 `0.55`。
- 松手：进入 `release`，倍率 `2.399`；2 秒后回到 `idle / 1 / 0.6 / 1`。
- 完整基线：加载 34,297 条原始数据与上游公开的原始 18MB `world.topo.jpg`，产品 UI 隐藏，原 dat.GUI 可见。
- 错误态：`?forceError=1` 显示完整可重载界面。
- 自动化捕获没有 page error 或 console error。

证据位于 `_qa/ui/`，结构化状态为 `_qa/ui/playwright-state.json`。
